From b2ffe3090d9d0edff9b9bc85b6a5528786cad36c Mon Sep 17 00:00:00 2001
From: Etaash Mathamsetty
 <45927311+Etaash-mathamsetty@users.noreply.github.com>
Date: Sun, 16 Aug 2026 22:56:00 -0400
Subject: [PATCH 58/59] HACK: winewayland: Introduce option to force using an
 offscreen window.

---
 dlls/winewayland.drv/wayland_surface.c | 12 +++++++++++-
 1 file changed, 11 insertions(+), 1 deletion(-)

diff --git a/dlls/winewayland.drv/wayland_surface.c b/dlls/winewayland.drv/wayland_surface.c
index 8c6dc1670a4..c6b4178361c 100644
--- a/dlls/winewayland.drv/wayland_surface.c
+++ b/dlls/winewayland.drv/wayland_surface.c
@@ -1466,10 +1466,20 @@ static BOOL needs_client_window_clipping(HWND hwnd)
     return ret > 0;
 }
 
+static LONG force_use_offscreen(void)
+{
+    static LONG ret = -1;
+    const char *env;
+
+    if (ret == -1) ret = (env = getenv("WAYLANDDRV_OFFSCREEN")) && !strcmp(env, "1");
+
+    return ret;
+}
+
 BOOL wayland_client_surface_update_offscreen(struct wayland_client_surface *surface)
 {
     HWND hwnd = surface->client.hwnd, toplevel = NtUserGetAncestor(hwnd, GA_ROOT);
-    LONG offscreen = FALSE;
+    LONG offscreen = force_use_offscreen();
     DWORD pid;
 
     if (NtUserGetWindowThread(toplevel, &pid) && pid != GetCurrentProcessId())
