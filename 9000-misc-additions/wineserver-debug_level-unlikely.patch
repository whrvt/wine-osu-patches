diff --git a/dlls/ntdll/unix/server.c b/dlls/ntdll/unix/server.c
index 108fd2081d5..2e7db381f6d 100644
--- a/dlls/ntdll/unix/server.c
+++ b/dlls/ntdll/unix/server.c
@@ -1739,7 +1739,7 @@ size_t server_init_process(void)
         req->unix_tid    = get_unix_tid();
         req->reply_fd    = reply_pipe;
         req->wait_fd     = data->wait_fd[1];
-        req->debug_level = (TRACE_ON(server) != 0);
+        req->_debug_level = (TRACE_ON(server) != 0);
         req->page_size   = get_host_page_size();
         wine_server_set_reply( req, supported_machines, sizeof(supported_machines) );
         if (!(ret = wine_server_call( req )))
diff --git a/server/main.c b/server/main.c
index 931ff1a7fa4..28fb71dc1d2 100644
--- a/server/main.c
+++ b/server/main.c
@@ -42,7 +42,7 @@
 #include "unicode.h"
 
 /* command-line options */
-int debug_level = 0;
+int _debug_level = 0;
 int foreground = 0;
 timeout_t master_socket_timeout = 0; /* master socket timeout, default is 3 seconds */
 const char *server_argv0;
@@ -71,9 +71,9 @@ static void option_callback( int optc, char *optarg )
     {
     case 'd':
         if (optarg && isdigit(*optarg))
-            debug_level = atoi( optarg );
+            _debug_level = atoi( optarg );
         else
-            debug_level++;
+            _debug_level++;
         break;
     case 'f':
         foreground = 1;
diff --git a/server/object.h b/server/object.h
index 7b40776c671..20f7dd3c94b 100644
--- a/server/object.h
+++ b/server/object.h
@@ -353,7 +353,12 @@ extern struct object *create_symlink( struct object *root, const struct unicode_
 /* global variables */
 
   /* command-line options */
-extern int debug_level;
+extern int _debug_level;
+#ifdef __GNUC__
+#define debug_level (__builtin_expect(_debug_level, 0))
+#else
+#define debug_level _debug_level
+#endif
 extern int foreground;
 extern timeout_t master_socket_timeout;
 extern const char *server_argv0;
diff --git a/server/protocol.def b/server/protocol.def
index be574accb23..81892c0e226 100644
--- a/server/protocol.def
+++ b/server/protocol.def
@@ -1178,7 +1178,7 @@ typedef volatile struct
 @REQ(init_first_thread)
     int          unix_pid;     /* Unix pid of new process */
     int          unix_tid;     /* Unix tid of new thread */
-    int          debug_level;  /* new debug level */
+    int          _debug_level;  /* new debug level */
     int          reply_fd;     /* fd for reply pipe */
     int          wait_fd;      /* fd for blocking calls pipe */
     unsigned int page_size;    /* client page size */
diff --git a/server/thread.c b/server/thread.c
index d6a455b53ee..ef98ee886f8 100644
--- a/server/thread.c
+++ b/server/thread.c
@@ -1819,7 +1819,7 @@ DECL_HANDLER(init_first_thread)
 
     set_thread_base_priority( current, current->base_priority );
 
-    debug_level = max( debug_level, req->debug_level );
+    _debug_level = max( debug_level, req->_debug_level );
 
     reply->pid          = get_process_id( process );
     reply->tid          = get_thread_id( current );
